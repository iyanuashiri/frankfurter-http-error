schema_version = "1.3"
artifacts = []

[task]
name = "Andela/frankfurter-http-error"
description = "Agent must find and fix an off-by-one in HTTP error-status detection that lets 400 responses pass through as valid data."
authors = [{name = "Iyanuoluwa Ajao", email = "iyanuoluwa.ajao@talent.andela.com" }]
keywords = ["python", "fastapi", "api"]

[metadata]
category = "debugging"

[verifier]
timeout_sec = 600.0
collect = []

[verifier.env]

[agent]
timeout_sec = 600.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]
